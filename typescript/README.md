# Entry test shopping cart

### Design
- Refrain from inheritance between normalItem, perishableItem and cartItem
- Pure functions

### Setup
- `$ yarn add -D jest typescript ts-jest @types/jest ts-node`
- `$ yarn ts-jest config:init`
- `$ yarn add eslint --dev`
- `$ yarn create @eslint/config`
